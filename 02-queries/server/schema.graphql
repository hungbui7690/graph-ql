type Query {
  job(id: ID!): Job
  jobs: [Job!]

  # (1b)
  company(id: ID!): Company
}

type Company {
  id: ID!
  name: String!

  # (1a)
  description: String
}

type Job {
  id: ID!
  title: String!
  company: Company!
  description: String
}
